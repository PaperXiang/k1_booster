# 变更报告: 删除守门员 guard 模式 + enable_auto_visual_defend（死代码清理）

- 日期 / 作者：2026-06-26 / 与 PaperXiang 结对
- 类型：删除（死代码 / 不可达分支）
- 默认是否生效：**行为中性**——删的都是当前不会执行的死分支，删前删后比赛行为一致
- 回退：`git revert` 本提交

## 1. 删了什么 / 为什么

| 删除项 | 文件 | 为什么是死代码 |
|---|---|---|
| 守门员 `guard` 分支 | `subtree_goal_keeper_play.xml` | `goalie_mode` 全代码恒为 `"attack"`，guard 分支永不进入。一并去掉 attack 块上已无意义的 `_while="goalie_mode=='attack'"`（现守门员单一 attack 模式） |
| `enable_auto_visual_defend` 整套 | `config.yaml`(删项) + `brain.cpp`(删 declare) + `brain_tree.cpp` `GoalieDecide`(删 get_parameter、相关 5 个变量、空 else-if 分支) | 该分支体已被删空、**不设 decision**；默认 false 不进入，且**一旦置 true 会输出空决策**导致守门员行为异常。属危险死代码 |

删除后 `GoalieDecide` 决策梯：`find → retreat → chase → kick → adjust`（干净）。

## 2. 没删什么（按你的规则）
- **`safe_shoot`**：`git log -S` 确认它来自**第一个提交 `bea87e5`（基线），不是新增**。你的规则是"新增且无用才删"——它无用但非新增，**故保留**（注释仍在）。
- **`goalie_mode` 的 C++ 写入**（`initEntry`、Intercept 节点里 setEntry "attack"）：现已无任何 XML 读取它，属**残留无害写入**；未动（删它需碰 Intercept 节点，收益低风险高）。如需"彻底干净"可后续单独清。

## 3. 风险
- 低：删的是不可达 / 关闭且空的分支，编译后行为应与之前逐帧一致。
- 唯一注意：动了 C++（`brain.cpp`/`brain_tree.cpp`）+ XML/config → **重编 brain + 同步整个 install/**。

## 4. 验证
- `colcon build --packages-up-to brain` 编过；起 `game.xml` 不崩。
- 守门员行为与之前一致（本就只跑 attack 模式）。
- grep 确认：无 `enable_auto_visual_defend` 残留、XML 无 `goalie_mode`、标签平衡。

## 5. 关于 A/B 能否 config 快速启用（同问澄清）
- **A（z=0 投影 / ground_plane）**：**不是 brain 的 config 开关**。`position_projection` 由 **vision 节点**算（z=0），换动态地面平面是 vision 侧改动，不能在 brain config 里一键开。
- **B（朝向校正 / SelfLocateCircle）**：当前**不是 config.yaml 开关**，是在 `subtree_locate.xml` **取消一行注释**即启用（快，但属 XML 非 config）。如需要可把它包成 config 开关。
